Instruction pour le compil: ./compil.sh nameLex.l nameYacc

TODO:
erreur seg fault lors de l'erreur "id a deja ete declare"

ERREURS GRAVES : 

- pour les structure recursive, on ne peut pas faire struct->champs1->champs1 (car struct->champs1 est un type STRUCT(NULL))
- nom des fonctions / variables

A DEMANDER :


NOUS SOMMES PERPLEXE:
typage des fonctions (en considerant que si on a un pointeur dans '(' declarator ')' , c'est que c'est un ptr sur fct )
le free pour la concatenation ?

CHOSES A TESTER:

- imbrications etc
